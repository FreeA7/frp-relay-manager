# Deploy

Deployment templates and server-side operational files.

This tree is copied to `kchat:/src/frp_relay/deploy` together with `server/`.
It should contain templates for nginx, systemd, and frp, plus install or
verification notes that are safe to commit.

Do not commit real secrets or generated production configs containing tokens.

# frp

frp server templates and notes.

Expected defaults:

- frps control port: `7000`
- frps dashboard/API: `127.0.0.1:7500`
- TCP/UDP remote port pool: `20000-49999`

Keep real frps auth tokens out of Git.
